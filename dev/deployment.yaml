apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-deployment
spec:
  selector:
    matchLabels:
      app: myapp
  replicas: 1
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
        - name: myapp
          image: picher/argocd-demo:latest
          ports:
            - containerPort: 9090
          resources:
            requests:
              cpu: 200m
            limits:
              cpu: 500m
